const dotenv = require('dotenv');

// dotenv.config({ path: './config' });

module.exports = {
  username: 'uainotes',
  password: 'uainotes',
  database: 'uainotes',
  host: '127.0.0.1',
  dialect: 'mysql',
  logging: false,
};
