const dotenv = require('dotenv');

// dotenv.config({ path: './config' });

module.exports = {
  username: 'root',
  password: 'Uai#2020',
  database: 'crud_sequelize',
  host: '127.0.0.1',
  dialect: 'mysql',
};
